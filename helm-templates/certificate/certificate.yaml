apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: letsencrypt-prod
  namespace: monitoring
spec:
  secretName: letsencrypt-prod
  dnsNames:
    - ${api_dns_name}
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
